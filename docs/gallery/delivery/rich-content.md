# 富文本扩展与发布时间交付

2026-09-14，依据用户已确认的 ADR 0013 实施，正式本地环境已接入。

## 实际完成

- Tiptap 3.31.3 增加表格、嵌套任务清单、下划线、删除线、高亮、行内代码和代码块。表格支持行列增删、表头、合并拆分和列宽调整；选中表格才显示相关工具，较少使用的样式集中于“更多格式”。
- 服务端校验和公开渲染同步扩展，保留原 schemaVersion 1 文档兼容。表格限制 100 行／20 列，检查矩形、跨度和列宽；代码按文字转义，任务状态由作者编辑，前台只读。手机端表格与代码块局部横向滚动。
- 外链图片转为可替换占位，不保存或请求远程 URL；点选占位后通过图库选片或上传素材替换。提示未导入图片、不支持内容及统一排版；正文标题归一为 H2/H3。Gallery 自身图片引用可往返复制，服务端仍检查引用和公开资格。
- 正式文章列表移除关于配置，仅保留关于文章标记；关于选篇集中到站点设置。后台导航与标签页标题为“照片标签”。
- 首次发布时间来自最早发布记录，最近更新时间来自当前发布版本；草稿保存、下线不改变公开时间。后台显示两个时间，未发布时标明“尚未发布”；前台首次发布后显示发布时刻，有后续发布时才显示更新。时间统一为北京时间 UTC+8。手填 date 改名“写作日期”，保留数据；记录列表按首次发布倒序。

## 验证

- `gallery:check` 无错误、无警告；候选及正式 `gallery:build` 通过；`gallery:test` 37 项通过。
- `gallery:test:db` 25 项通过，覆盖 0009、受限运行角色、富文本保存与公开 JSON 一致性、首次时间保持、发布快照、关于约束、媒体撤回和完整数据库／素材恢复。Immich 原迁移及结构漂移为 0 项。
- 使用意大利 demo 在独立样例里验证粘贴：5 行 4 列表格、5 项任务及 2 项勾选、5 处高亮、代码块／行内代码、删除线均保留，2 张外链图片变成占位。表格行列增删、合并拆分和任务状态编辑通过浏览器操作。
- 真实后台以未发布临时草稿验证整篇粘贴、保存、刷新、占位替换、预览、关于设置归属、照片标签及已有文章发布时间。1440／390／320 像素无页面横向溢出和脚本异常，临时草稿已删除，用户既有文章未改动或发布。
- 本地截图 `.gallery-local/verification/articles-rich/`；本轮不包含真实手机硬件和公网部署验收。

## 本地环境与维护

0009 仅为两个公开视图追加 first_published_at，没有新表、正文重写或 Immich 结构修改。迁移前完整备份 `.gallery-local/backups/before-rich-content-2026-09-14T09-11-33.150Z.dump`，权限 0600，并完整解析校验。前后台已重新构建、启动。

依赖仅新增／直接声明三个同版本扩展，pnpm-lock 仅含 Gallery 新增项。Node、pnpm、上游解析和用户已有 mise.lock 不变。按维护说明暂停 Immich API/Web、冻结同步宿主机和 Linux 依赖卷后恢复；API ping 与 Web 正常，数据库与媒体未改。

此前粘贴时丢失的结构无法自动恢复，需从原文重新粘贴。`/design/*` 是历史样例，正式写作和配置使用 `/articles`、`/settings`。新节点无法由旧版基础渲染器读取；回退需保留新版读取能力，或在隔离环境恢复匹配的数据与代码。数据库和独立素材继续成对备份。
