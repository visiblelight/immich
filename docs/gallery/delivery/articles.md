# 记录模块交付

2026-09-14。产品范围已确认，视觉评审与真实业务交付分别记录。

## 第一阶段：样例评审（历史记录）

- 已实现样例：记录列表、长文阅读与标题目录、纯文字／关于样稿、图片沉浸浏览；后台列表、标题／摘要／日期／封面／关联相册、Tiptap 富文本、图注直接编辑、示例照片多选和本机图片插入。
- 可操作的样稿流程：IndexedDB 保存文章和插图，刷新恢复；预览当前草稿／发布样稿，模拟发布／更新／下线与关于选篇。浏览器内数据与真实服务端完全隔离，不能视为正式发布功能。
- 已实现内容基础：结构化文章类型、内容白名单与容量／深度限制、链接协议和文字转义、图片引用解析接口、SSR 轻量渲染。公开端不加载 Tiptap。
- 正式站点尚未增加文章入口，样例中的五项导航用于本轮视觉确认；不会把虚构文章混入正式内容。
- 文档：ADR 0012 及文章数据／素材授权设计。
- 本阶段使用虚构文章与既有样例图片；不修改真实相册或照片，不迁移开发数据库。

## 第一阶段时的后续安排

视觉与写作体验评审后，接入文章数据库、素材上传存储、自动保存与并发冲突、发布／下线、关于选篇、相册双向关联及完整隔离验收。样例内任何保存／预览都不是正式发布。

## 预览入口

- 前台记录：http://127.0.0.1:3100/design/records
- 游记阅读：http://127.0.0.1:3100/design/records/a-road-through-georgia
- 纯文字：http://127.0.0.1:3100/design/records/a-little-time-for-nothing
- 关于选篇样式：http://127.0.0.1:3100/design/records/about
- 后台文章：http://localhost:3101/design/articles

后台列表点文章即可试写，插图支持已提供的示例照片及本机 JPEG/PNG/WebP。本阶段上传保存在浏览器 IndexedDB，不上传到服务器，也不执行正式素材清理元数据管道。换浏览器或清除站点存储不会保留样稿；两个前后台样例不通过浏览器存储同步，编辑结果在后台“预览”查看。单机样例不承诺多标签页并发；正式服务端将使用 draft_version。

## 验证

- `gallery:check`：0 错误、0 警告。
- `gallery:test`：35 项通过（含新增 4 项文章内容／链接／引用／复杂度测试）。
- `gallery:build` 与 `gallery:smoke`：生产构建、设计开关、文章路由、无配置服务边界通过。
- Chromium 实测 1440、390、320 像素：无横向溢出或页面异常；正文输入、图注、照片插入、本机素材、阅读预览、发布样稿隔离及刷新后文件恢复通过。
- 截图保存在本机 `.gallery-local/verification/articles/`，不提交虚构验收样稿到真实数据库。

## 开发环境

Tiptap 3.31.3 仅新增于 Gallery 后台，保留原 Node/pnpm、Immich 依赖解析和用户 mise.lock 修改。依赖安装按挂载手册暂停并恢复既有 Immich API/Web；容器固定锁文件安装后 API ping 已返回 pong。现有数据库与媒体未修改。后续删除样例路由即可回退本阶段；没有数据库回滚步骤。


## 第二阶段：真实业务接入（2026-09-14，已完成本地验收）

用户已认可样例的视觉与写作体验并授权继续。新增正式 `/records`、`/records/[slug]`、后台 `/articles`、`/articles/[id]`；页面保持已评审的阅读与编辑布局。共用导航已加入记录／文章，关于可选已发布文章，相册末尾显示相关文章。

已实现：文章服务端自动保存与乐观锁、预览、发布／更新／下线、删除从未发布的草稿；图库照片可按相册／标题／标签检索；文章素材真实上传、复用、分页检索、查看引用数和删除未引用素材；正文与封面均纳入发布授权；站点关于选篇使用独立版本号。该流程不导入上一阶段浏览器中的示例文章。

0008 包含 6 张新表、2 个站点字段、5 个公开视图及发布引用防删除触发器。公开角色未获基础表权限。Compose 增加独立文章素材挂载，前台只读、后台可写；后台请求上限设置为 12M，图片接口自身最多 10 MiB。没有新增依赖或改变 Immich 数据表、媒体和版本。

### 验证与开发环境

- `gallery:check`：0 错误、0 警告；`gallery:build`、`gallery:smoke` 通过；`gallery:test` 35 项通过。
- `gallery:test:db`：25 项通过。新用例覆盖素材去元数据、类型拒绝、当前版本授权、草稿隔离、乐观锁、发布不可变、关于下线保护、来源相册撤权、历史引用保留和删除边界。真实 HTTP 覆盖大于 512 KiB 的认证上传、发布／下线／再发布及关于快照。
- 完整 pg_dump／pg_restore 后比较文章、版本、引用及内容摘要，再从复制的素材目录用公开角色读取图片。Immich 原迁移与结构漂移检查为 0 项。
- Chromium 在当前真实开发环境验证登录、创建草稿、自动保存、刷新恢复、图库照片与上传插图混排、阅读预览、草稿媒体拒绝、删除草稿与未引用素材；1440、390、320 像素页面无横向溢出，无页面脚本异常。临时内容均已清理，未发布到真实前台。
- 本地完整备份：`.gallery-local/backups/before-articles-2026-09-14T07-58-17.306Z.dump`，权限 0600；完整解析验证后应用 0008。独立素材目录 `.gallery-local/article-media`，前后台 env 已配置，后台请求上限 12M。Immich 数据表与媒体未更改。
- 前后台已重新构建并启动；本地截图 `.gallery-local/verification/articles/real-*.png`。Compose 配置与挂载已更新，本轮未重新部署完整 Compose 镜像；公网、真实手机和大规模长文体验不包含在本地验收结论内。

### 正式使用入口

- 后台文章：http://localhost:3101/articles
- 前台记录：http://127.0.0.1:3100/records
- 关于选篇：文章列表下方或站点设置中的“关于页面”；先发布文章，再选择并应用。若不希望关于文章出现在记录列表，取消文章设置中的“展示在记录列表”，再发布。

原 `/design/*` 保留为独立样例。正式记录列表初始为空，不导入样例和浏览器 IndexedDB 内容。用户发布后才会出现自己的文章。

### 维护与回退

0008 是追加迁移，不重写旧相册和照片正文。运行代码要求新结构及文章素材路径。数据库和素材文件必须成对备份，见维护手册；旧发布引用保留，后台只允许删除完全未引用的素材，没有自动清理任务。代码回退可保留新增表与文件；不要直接恢复旧完整数据库覆盖后续写入，恢复应先在隔离环境验证。
