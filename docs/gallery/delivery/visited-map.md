# 去过功能交付记录

日期：2026-09-12。产品方案已确认，地图视觉仍是独立验收节点。

## 本阶段

- 已记录已确认规则：只统计公开且允许位置的 Gallery 照片；自动推导到访并允许后台人工校正。
- 新建关键区域原创交互样稿 `/design/visited`，只在 GALLERY_DESIGN_PREVIEW=1 时开放，no-store / noindex。
- 样稿展示黑海、里海、东地中海、红海、波斯湾及周边国家。国家面积是区域裁切后的示意，不代表完整世界面积验收。
- 到访日期、数量和上色均为明确标注的示例，不连接数据库，不表示用户实际旅行记录。
- 悬浮/聚焦摘要、点选面板、缩放与平移用于评审；真实国家地图按钮明确为后续接入状态。

## 尚未完成

完整世界地图及国家清单验收；真实国家边界选型与统计；到访校正后台；OSM 国家照片地图；Google/高德配置和授权 Key 联调；正式导航上线。尚未新增数据库迁移或更改运行库。

## 验证

- `gallery:check`：五个 Gallery 包通过，零错误、零警告。SVG 自定义交互有明确的键盘替代操作，并对 Svelte 的静态非交互元素识别作局部说明。
- `gallery:test`：15 项通过，新增 3 项几何检查，验证高加索相对方位、共享边界、海域通道、标签落点、密集采样无重叠，以及区域裁切不把阿富汗画成沿海国家。
- `gallery:build`：公开站点与后台生产构建通过。
- `gallery:smoke`：公开/后台普通模式与预览模式共 4 组通过；新样稿纳入开关验证，关闭时 404，开启时 200 且带 no-store/noindex。
- Chrome 桌面实测：国家悬浮摘要、两次到访记录、点击面板、键盘 Enter 选择、局部放大正常。
- 390×844 手机视口：无横向溢出，底部信息面板与关闭按钮正常；通过浏览器触摸事件验证点选、拖动和双指缩放，后两者未误打开国家。不是实体手机硬件验收。
- 本地预览：`http://127.0.0.1:3100/design/visited`，无需后台登录。测试用 3102 进程收尾停止，保留日常前台 3100；未重启后台或 Immich。

下一步为用户确认本阶段轮廓与交互方向，再展开完整世界地图及真实数据业务。真实国家地图按钮在样稿中禁用并注明后续接入，不作为已完成功能。

## 用户确认后继续实施（2026-09-12）

上面的“本阶段／尚未完成”是区域样稿交付时的历史状态。用户随后确认“看起来可以，继续”，本次实际进展如下。

### 已接入

- 完整195项世界轮廓，海域留白、完整名称、统一共享边、平移缩放、国家摘要和国家列表；区域样稿继续保留，完整轮廓示例为 `/design/visited/world`。
- 正式导航增加去过，`/visited` 接入真实公开照片。动态国家归属、去重、保守位置精度、多次到访与未知日期处理。
- `/visited/[country]` OSM真实底图、完整计数的聚合、分页照片、到访筛选、大图／照片组查看、返回地图保存中心与缩放等URL状态。
- 后台 `/maps` 底图配置与到访间隔，`/visits` 日期校正、合并、拆分、恢复自动整理；编辑在独立弹窗，包含键盘焦点限制及移动布局。
- Google Maps和高德SDK适配代码；高德官方坐标转换及反向视口转换；受限官方代理；安全码AES-256-GCM密文存储，公私配置明确投影。
- 0006四表与数据字典同步；未改变Immich表、迁移或媒体。

### 本次验收

- gallery:check：零错误、零警告；gallery:test：22项通过。
- gallery:test:db：23项通过，真实PG14角色、GPS移国、去重、证据撤回、人工校正失效、配置秘密过滤、乐观并发、备份恢复均通过。包括10000照片既有聚合性能用例；完整Immich迁移／漂移复核为0项。
- gallery:build与gallery:smoke通过；新增完整世界样稿受预览开关控制。
- 整图邻接复核修正了旧原型中格鲁吉亚误接伊朗、亚美尼亚未接阿塞拜疆及哈萨克斯坦误接蒙古的问题，添加共享边回归测试。完整195项清单、名称、主要海域及高加索方位检查通过；不宣称所有世界边界已逐一人工审查。
- 浏览器实测真实世界摘要→格鲁吉亚OSM→聚合成员→照片组大图→返回原地图；底图与缩略图成功显示，无浏览器错误日志。后台设置、国家筛选、编辑弹窗实测。390像素视口的后台弹窗宽370像素，无横向溢出。
- 本轮内置浏览器不支持CDP触摸事件，无法补做整图真实触摸事件验收；区域原型上一阶段已有Chrome触摸点选／拖动／双指缩放验证。本轮响应式检查不等于实体手机验收。

### 本地应用与保护

0006迁移前备份：`.gallery-local/backups/before-visited-2026-09-12T05-23-44.128Z.dump`，权限0600。对album、album_draft、album_photo、album_release、album_release_photo、asset_entry内容做迁移前后摘要比较，一致。没有向现有作品写入测试照片或人工行程。

前台 `http://127.0.0.1:3100/visited`；后台 `http://localhost:3101/maps` 和 `/visits`。仅重启Gallery两项服务，未重启Immich。当前实际公开位置照片共2张，归属格鲁吉亚，其他位置隐藏照片不参与。

### 明确未完成的外部验收

Google/高德有效API Key、安全码及域名／账单授权尚未提供，不能宣称这两项真实服务、国内外控制点和切换精度已验证。后台保存只表示配置格式与密文校验通过；当前仅默认OSM可直接使用。Google正式部署还需自有Cloud Map ID替换官方示例标识。完整世界图细部视觉、更多地区拓扑、实际设备与公网网络验收继续保留为后续优化项。
