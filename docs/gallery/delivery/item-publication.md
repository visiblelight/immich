# 照片组交互与单项发布交付

状态：本地开发环境已实现并完成下述验收，2026-09-12。需求基线见 [ADR 0005](../decisions/0005-item-publication-and-viewer.md)。

## 已实现

- 前台组视图只显示组标题／说明，空说明不回退到个人说明；分享标题和描述同步使用组文案。个人内容仍保留在数据库中。
- 组内垂直缩略图放左侧，底部显示同相册项目缩略图；上下切视角、左右切项目、放大后平移。全站相片视图继续使用其筛选与排序上下文。
- 日期显示年月日、时分秒与 Immich 提供的时区；没有可用时区则显示“记录时间（时区未知）”。年月筛选读取当地钟表月份。
- 照片组在独立弹窗中创建和编辑，手机全屏；取消只丢弃弹窗副本。未保存关闭确认在弹窗内展示。成员可调整顺序、封面、画面描述和位置公开方式，保留拖拽浮层、占位和动画。
- 单张／单组提供保存草稿、保存并发布。后端以当前公开快照为基础合并选中范围，原子处理组关系；游记、其他照片、隐私策略和全册排序不被顺带发布。未公开相册或上级阻止直接发布，但允许保存草稿。
- 局部发布后保留其他本地未保存修改；数据库通过 has_unpublished_changes 区分剩余草稿。全册按钮明确为“发布相册全部修改”。

## 验证

运行根目录 Gallery 脚本：

- gallery:check：0 errors / 0 warnings。
- gallery:build：前后台生产构建通过。
- gallery:test：12 项通过。
- gallery:test:db：22 项通过。覆盖单项隔离、新照片成组、解散、并发冲突、失效来源、媒体损坏后草稿和发布共同回滚、祖先下线、脏状态和当地时间跨 UTC 月份。
- 真实角色权限、发布祖先、路径约束、GPS 实时变化、备份恢复及 Immich 原始迁移／结构检查通过，Gallery 引入的 Immich 结构差异为 0。
- gallery:smoke：生产与设计开关隔离通过；无配置服务仍拒绝业务就绪。

浏览器使用真实开发图库的隔离副本验证：取消编辑不改变相册；发布组后未保存游记仍在编辑器中；空组说明不显示个人说明；新建组、解散草稿、单张独立发布、组内拖拽浮层和占位；大图上下／左右切换。检查桌面与 390×844 移动视口。真实手机触控和公网部署不在本次验收范围。

## 本地迁移与访问

迁移 0005 已在独立副本通过后应用到本地开发库。事先使用数据库 owner 执行两列只读授权准备，再使用 gallery_migrator 迁移；运行服务不持有 owner／migrator 凭据。

迁移前备份：`.gallery-local/backups/before-item-publication-2026-09-12T02-01-57-485Z.dump`，18,524,959 字节，权限 0600。两本相册的草稿内容、照片记录和全部历史 release 内容摘要保持一致，历史发布照片记录为 20 条；没有把验收文案写入原图库。

前台 http://127.0.0.1:3100/albums；后台 http://localhost:3101/albums。原有账号和密码未更改。两个实际服务 `/health/ready` 均为 200，公开相册和后台登录页均为 200。临时预览及数据库容器已清理。

旧页面需重新载入以使用新发布规则；如果仍有未保存内容，先复制保留再刷新。原有用户浏览器页面未由本次验收保存或刷新。
