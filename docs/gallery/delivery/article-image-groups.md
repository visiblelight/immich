# 文章隐藏作品与图片组交付

日期：2026-09-21。用户已完成本地验收；已推送 GitHub 并完成生产迁移、CI 自动发布与公网验证。产品规则见 [ADR 0016](../decisions/0016-article-image-groups.md)，数据字典见 [文章结构](../architecture/articles.md)。

## 使用入口

- 后台相册 → 编辑照片 → “仅在文章引用时展示”。照片组编辑可批量设置全组，也可逐个成员设置。保存是草稿；发布后在所有相册共享生效，仍保留后台成员关系。
- 后台文章 → 图片 → Gallery 照片 / Gallery 照片组 / 上传素材。Gallery 选项包含来源相册已发布的隐藏作品；来源相册未发布时先在相册完成发布，隐藏作品不会因此出现在图库。
- 单张可逐张插入；跨相册及素材类型多选后“组成图片组”，拖动或箭头调整顺序，填写文章独立说明。重新编辑可增减与排序，至少两张。
- Gallery 照片组直接引用已有组，成员跟随来源组的发布更新；文章可以填写独立图注。临时组只跟随文章发布。
- 前台提供完整画面轮播、图片内浮动左右按钮、原生横向滚动/滑动和当前组内沉浸大图；无自动播放。文章素材继续不进入作品库。

## 权限与 CDN

新增 0011 迁移（仅 Gallery schema）。图库公开视图排除隐藏作品；文章专用媒体地址每次检查当前文章引用和来源相册、祖先、成员、来源资源及路径。移除引用、下线文章、来源组删除或来源相册下线会撤销对应授权。

CDN exporter 已纳入当前文章授权的 Gallery 展示图，内容寻址去重，图片仍使用 Immich 两种衍生图；没有新增原片访问。继续遵守已确认的旧签名最长 300 秒撤销边界。

## 实际验证

- `gallery:check`（0 错误、0 警告）、`gallery:test`（44 项）、`gallery:build`、`gallery:smoke`（4 种启动场景）通过。
- `gallery:test:db`：25 项真实 PostgreSQL 14 集成检查通过；包括临时混合组、现有组实时发布引用、隐藏图库/封面/相片、猜测文章与相册 ID、草稿不授权、组成员变更、HTTP 和 CDN 暖缓存撤销、发布历史不可改，以及完整 pg_dump/pg_restore 后隐藏状态与文章媒体访问。
- Immich 原始 migration/schema 检查 0 漂移。10,000 点地图查询约 39 ms；240 张相册分页约 71 ms（本机隔离合成数据，非生产性能承诺）。
- 浏览器实际验证：现有组插入、混合 Gallery 照片与普通上传素材、组内排序、重新打开编辑、跨素材来源保留选择、保存及独立预览、多个组内大图边界，390px 宽度布局。真机触摸手势未单独验证。
- 本地先备份到忽略目录 `.gallery-local/backups/before-article-groups-20260921.dump`，再用专用迁移角色应用 0011，重启 3100/3101；未修改 Immich 文件或表。
- 本地后台保留一篇“图片组功能验收（本地草稿）”及一张合成上传素材供体验，不发布到公网。

## 文章图片视觉修订

已按用户反馈统一单图与组图的正文排版：移除背景卡片、组名、数量和底部控制行，使用图片内悬浮箭头；手机保留轻量箭头和原生滑动。组图依据首张图片比例稳定布局，完整显示所有照片。只渲染文章明确填写的图注，不再自动带入来源组 Markdown（含标题、分隔线）。编辑器取消大块信息按钮，改为图片角落的编辑入口。

本轮 `gallery:check`（0 错误、0 警告）、`gallery:test`（45 项）、`gallery:build` 通过；新增回归验证来源描述不会生成标题/分隔线、显式图注仍安全输出。已在本地独立预览检查桌面和 390px 布局、箭头及键盘切换、编辑器图像和编辑弹窗；无横向页面溢出。触屏手势仍待真机验证。本地验收时已重启前后台，未修改用户文章内容；后续生产结果见下文。

## 生产迁移与发布记录

用户完成本地验收后明确授权推送上线。应用提交 `434f71f9e1ce29169bf57ad0cb4916897ec9aac8`，中文标题和详细正文，作者邮箱 `visiblelight@gmail.com`；未包含原有 `mise.lock` 改动。

- 生产私有备份：`/srv/vision/backups/before-article-groups-20260921T143753Z`，包含在线一致性全库 dump、私有配置、原镜像和提交记录、候选迁移及校验和。此次仅迁移数据库结构，不改动媒体文件，因此未另复制原片。没有停止 Immich 或同机其它项目。
- 通过专用 `gallery_migrator` 执行 Git 候选的 0011；前后 Immich public schema 比对一致，迁移后旧版前后台 readiness 仍正常。
- 权限核对：`gallery_public` 无权读取 `article_source_photo` 和 `article_group_ref`；只能读取发布授权后的媒体视图。
- 首次工作流 `35613334269` 完成全部检查与镜像构建，但在拉取镜像约四分钟无输出后 SSH 报 `Broken pipe`，应用未切换。清理残留下载客户端后，通过中文提交 `15d835316` 在 workflow 中加入 30 秒 SSH 心跳、10 次连续失败阈值及 20 秒连接超时，保留原权限与主机验证。
- 重试工作流 [35614421883](https://github.com/visiblelight/immich/actions/runs/35614421883) 全部成功。生产应用版本 `15d835316b664354f1c0f8f51f7312a71ac0d4b2`；固定镜像 `ghcr.io/visiblelight/gallery@sha256:23932a8fbe8077ac19cf3d8586c107db5cee1342e2d55486c2b9506bfdaebb27`。
- 两个生产容器标签及 `/srv/vision/releases/current` 与上述提交一致；public/admin readiness 正常，运行环境未包含迁移或 bootstrap 数据库凭据，服务器受控文件无未提交修改。
- 五个前台一级页面、后台登录、Immich、JVS 与统计站点 HTTPS 均返回 200。现有公开照片实测经 CDN 返回图片，响应保留 `no-store`；未知文章/照片引用返回 404。
- 新版本执行一次现有 CDN 同步任务成功：2 个对象，0 上传、0 删除、0 失败。同步逻辑从更新后的前台容器执行，无需重装宿主同步服务。
- 生产当前有 1 个公开相册、1 张公开照片、0 篇已发布文章。没有把本地测试稿搬到生产，也没有发布或修改用户文章；图片组交互与隐藏作品撤销的完整行为通过前述本地浏览器及隔离数据库验收，生产本轮核对版本、权限、现有图片链路和无效引用拒绝。
- 发布前后容器 ID 对比确认：只有 Gallery public/admin 被替换；Immich、数据库、Edge、JVS、Umami 均保持原实例。

## 回退边界

CI 对迁移历史的严格检查保持启用，不能只推新代码而忽略协调迁移。0011 是追加字段、引用表与视图更新；已有作品默认不隐藏。旧编辑器不认识新增图片组节点，回退前需禁止旧编辑器保存含新节点的文章；不要删除新增引用表或覆盖用户新内容。全库恢复会覆盖备份后写入，不能把它当作无损的应用回退。
